import { Settings } from 'react-feather';
import './styles.css';

export const Footer = () => {
    return (
        <div className="footer">
            <Settings color="#fff" size="{48}" />
        </div>
    );
};
